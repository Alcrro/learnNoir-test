// When you have the logo file, add it to src/assets/ and uncomment the import:
// import logo from "../assets/logo.svg";

export const BRAND = {
	name: "LearnNoir",
	description: "Step-by-step lessons for curious minds",
	logo: undefined as string | undefined, // replace `undefined` with `logo` after import
} as const;
